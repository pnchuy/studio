
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all collections for now
    // to ensure the app can display data.
    // Writes are disallowed to protect the data.
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
